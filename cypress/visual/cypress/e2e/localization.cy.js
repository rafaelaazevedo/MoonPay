/// <reference types="Cypress" />

//TODO: Create one scenario to compare all languages or one each or even the most used ones
// Create one scenario to change language on mobile and compare
describe("Localization in the checkout page", () => {
  it("should display the checkout page in chinese correctly", () => {});

  it("should display the checkout page in english correctly", () => {});
});
